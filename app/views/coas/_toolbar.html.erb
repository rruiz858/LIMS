<div class="divide-nav">
  <div class="navbar navbar-default navbar-static-top bottle-navbar">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".main-nav">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div class="collapse navbar-collapse main-nav">
        <ul class="nav navbar-nav" role="tablist">
          <li role="presentation" class="active"><a href="#all-coas-form"  class='show' data-toggle="tab">COAS
            <span class="sr-only"></span></a></li>
          <li role="presentation"><a href="#all-msds-form"  class="show" data-toggle="tab">MSDS
            <span class="sr-only"></span></a></li>
        </ul>
        <div class="nav navbar-nav navbar-right">
          <% if can? :manage, CoaUpload %>
              <a href="<%= new_coa_path %>"class="btn btn-success create-pdf" style='color:white'>New COA <i class="fa fa-upload"></i></a>
          <% end %>
          <% if can? :manage, MsdsUpload %>
              <a href="<%= new_msd_path %>"class="btn btn-success create-pdf" style='color:white'>New MSDS <i class="fa fa-upload"></i></a>

        <% end %>
        </div>
      </div>
    </div>
  </div>
</div>