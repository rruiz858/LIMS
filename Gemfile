source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.1'
# Use mysql as the database for Active Record
gem 'mysql2'

gem 'rake', '~> 10.4.2'
# Use SCSS for stylesheets
#gem 'bootstrap-sass', '~> 3.3.5'
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# See https://github.com/rails/execjs#readme for more supported runtimes
gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
# gem 'turbolinks' Removed turbolinks because of issues with coffee scripts not loading
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# gem for authentication
gem 'devise', '~> 3.5.1'

# gem for authorization
gem 'cancancan', '~> 1.12.0'

#gem for uploading things
gem 'carrierwave'

gem 'sprockets'

#gem to read and write spreadsheets
gem 'roo-xls'

gem 'net-ldap'

#gem to paginate records in view
gem 'will_paginate'


gem 'simple_form'
#gem is utilized for country/state library for addresses

gem 'countries', '~> 1.2.2'


gem 'delayed_job'
gem 'delayed_job_active_record'
gem 'progress_job'
gem 'daemons'
gem 'wicked_pdf'
gem 'wkhtmltopdf-binary'

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console

  gem 'byebug'

  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  gem 'factory_girl'
  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  # gem 'spring'

  # ruby prof is our profiler in dev.
  gem 'ruby-prof'
  gem 'mocha'
  gem 'capybara'
  gem 'selenium-webdriver'
  gem 'database_cleaner'
  gem 'better_errors'
end

